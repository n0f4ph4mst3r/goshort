HTTP_SERVER_PORT="8080"

DATABASE_URL="postgres://postgres:test@postgres:5432/main?sslmode=disable"
CACHE_URL="redis://redis:6379/0"

# use these variables, if run app manually
HTTP_SERVER_ADDRESS="localhost"

CONFIG_PATH="/path/to/your/config.yaml"

# use this path for SSO authentication
# SSO_PUBLIC_KEY_PATH="/path/to/your/public/key"

# use these variables for your docker containers

HOST_CONFIG_PATH="./config/sample.yaml"
HOST_SSO_PUBLIC_KEY_PATH ="/path/to/your/public/key/on/host/machine"

POSTGRES_USER="postgres"
POSTGRES_PASSWORD="test"
POSTGRES_DB="main"
POSTGRES_PORT="5432"